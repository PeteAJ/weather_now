require 'spec_helper'

describe WeatherNow do
  it 'has a version number' do
    expect(WeatherNow::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
